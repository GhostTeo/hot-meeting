// Copia questo file come `config.js` e usa esclusivamente la chiave pubblica anon.
// Non inserire mai una chiave service-role nel browser.
export const supabaseConfig = {
  url: 'https://YOUR_PROJECT.supabase.co',
  anonKey: 'YOUR_PUBLIC_ANON_KEY'
};
